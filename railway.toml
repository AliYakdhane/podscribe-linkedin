name = "podscribe-linkedin-backend"

[build]
providers = ["apt"]

[build.apt]
packages = ["ffmpeg"]

[services.backend]
  # FastAPI backend service
  root = "."
  start = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"

