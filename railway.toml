name = "podscribe-linkedin-backend"

[build]
providers = ["apt"]

[build.apt]
packages = ["ffmpeg"]

[build.python]
# Your Python config here if needed

[services.backend]
root = "."
start = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"